{
  "name": "sushi-ordering-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build1": "npm install vite ",
    "build2": "npm install",
    "heroku-postbuild": "npm run build1 && npm run build2 && npm run build ",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  },
  "dependencies": {
    "aos": "^2.3.4"
  }
}