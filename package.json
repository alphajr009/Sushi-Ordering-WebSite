{
  "name": "sushi-ordering-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "heroku-prebuild": "export NPM_CONFIG_PRODUCTION=false; npm install --only=dev",
    "heroku-postbuild": "npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  },
  "dependencies": {
    "aos": "^2.3.4"
  }
}